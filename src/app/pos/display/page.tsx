import DisplayClient from "../display-client";

export const metadata = {
  title: "POS Display",
  description: "Customer display for payments",
};

export default function DisplayPage() {
  return <DisplayClient />;
}
